language: go

dist: trusty
sudo: true

install:
  - sudo apt-get update
  - sudo apt-get install hugo

script:
  - hugo

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: ./public/
  on:
    branch: master